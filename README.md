# The Wall: Social Media Feed

Social feed app.

## Setup

```shell
pipenv install
pipenv run python seed.py
```

## Usage

Start the server and access the API.

```bash
pipenv run uvicorn main:app --reload
```
